const sectionCreator = (sectionTitle) => {
  let id = crypto.randomUUID();
  let sec = [];
  let title = sectionTitle;

  const getSec = () => sec;
  const getId = () => id;
  const getTitle = () => title;

  //sec

  const pushSec = (secArray) => {
    secArray;
  };
};
